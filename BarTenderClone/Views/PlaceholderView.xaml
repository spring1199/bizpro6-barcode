<UserControl x:Class="BarTenderClone.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BarTenderClone.Views"
             xmlns:vm="clr-namespace:BarTenderClone.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:PlaceholderViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource BackgroundBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Icon / Emoji -->
            <TextBlock Text="{Binding Icon}" 
                       FontSize="72" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,24"/>
            
            <!-- Title -->
            <TextBlock Text="{Binding Title}" 
                       FontSize="24" 
                       FontWeight="SemiBold" 
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,8"/>
            
            <!-- Message -->
            <TextBlock Text="{DynamicResource PlaceholderMessage}" 
                       Style="{StaticResource CaptionTextStyle}" 
                       FontSize="14" 
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
