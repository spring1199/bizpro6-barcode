<UserControl x:Class="BarTenderClone.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:vm="clr-namespace:BarTenderClone.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="{DynamicResource BackgroundBrush}" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="{DynamicResource SettingsHeader}" 
                   FontSize="28" FontWeight="Bold" 
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   Margin="0,0,0,24"/>

        <!-- Settings Content Card -->
        <Border Grid.Row="1" Style="{StaticResource PanelStyle}" VerticalAlignment="Top" MaxWidth="600" HorizontalAlignment="Left">
            <StackPanel Margin="8">
                <TextBlock Text="{DynamicResource SettingsGeneral}" 
                           FontSize="16" FontWeight="SemiBold" 
                           Foreground="{DynamicResource TextPrimaryBrush}" 
                           Margin="0,0,0,20"/>

                <!-- Language Selection -->
                <Grid Margin="0,0,0,16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="250"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="{DynamicResource SettingsLanguage}" 
                               Style="{StaticResource BodyTextStyle}" 
                               VerticalAlignment="Center" 
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                    
                    <ComboBox Grid.Column="1" 
                              ItemsSource="{Binding AvailableLanguages}"
                              SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}"
                              DisplayMemberPath="DisplayName"
                              Style="{StaticResource ModernComboBoxStyle}"
                              Height="36"
                              VerticalAlignment="Center"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
